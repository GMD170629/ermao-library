[
  {
    "stepTitle": "升级二毛图书",
    "items": [
      {
        "type": "radio",
        "field": "wizard_install_mode",
        "label": "安装方式 / Installation mode",
        "initValue": "update",
        "options": [
          { "label": "全新安装 / Fresh install", "value": "fresh" },
          { "label": "更新 / Update", "value": "update" }
        ],
        "rules": [
          { "required": true, "message": "请选择安装方式 / Select an installation mode" }
        ]
      },
      {
        "type": "tips",
        "helpText": "全新安装会直接清空应用配置及数据（账户、书库配置、阅读进度、封面、索引、日志和会话密钥），不备份；原始书库文件保留。更新保留原有数据。0.x 无法自动升级到 v1，必须选择全新安装；选择更新将在提交后提示并终止。 / Fresh install permanently erases application settings and data (accounts, library settings, reading progress, covers, indexes, logs and session secrets), without a backup. Original library files are retained. Update preserves existing data. Upgrading from 0.x to v1 requires Fresh install; choosing Update will display an error and stop installation after submission."
      },
      {
        "type": "text",
        "field": "wizard_port",
        "label": "Web 访问端口",
        "initValue": "3000",
        "rules": [
          {
            "required": true,
            "message": "请输入 Web 访问端口"
          },
          {
            "pattern": "^(102[4-9]|10[3-9][0-9]|1[1-9][0-9]{2}|[2-9][0-9]{3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])$",
            "message": "请输入 1024-65535 之间的端口"
          }
        ]
      },
      {
        "type": "tips",
        "helpText": "共享书库目录“/shuku.library”会挂载为应用的 /libraries/books 书库根目录。升级后请使用所选独立端口访问应用。"
      }
    ]
  }
]
