# Agent Working Guidelines

## 适用范围与权威来源

本文件规定全仓代理行为、工程底线和专项文档入口。细则按任务读取，不把所有专项规格重复加载为全局规则。用户当前明确要求优先；专项行为以当前机器契约、已接受且未被取代的 ADR 和对应规范为准，旧计划、截图、历史测试和遗留实现不能覆盖新要求。若当前权威来源仍冲突，先定位冲突并说明，不自行发明兼容规则。

目标架构不代表存量代码已经合规。新增能力遵守目标；遗留缺陷优先在现有职责位置最小修复，不因触及旧代码自动迁移。必要重构须与直接原因或明确需求相关。实现细则见 [工程实现规范](docs/engineering-standards.md)，实际能力入口见 [当前代码结构](docs/business-code-layering-and-refactoring.md)。Cursor 规则只作读取入口，不维护第二套细则。

## 默认执行与停止条件

确认具体失败 → 定位直接原因 → 修改现有实现 → 定向验证原失败及受影响边界 → 结束该问题或继续下一项。原因、预期和影响范围明确时直接修复，不重新讨论整套架构或反复证明已确认事实。风险按实际行为变化和调用范围判断，不按“跨端、离线、并发、持久化”等词或改动行数判断。

原失败已适当验证、关键相邻行为未被破坏、范围清楚且无具体证据指向未解决相关风险时，结束该问题。理论可能性不足以继续扩验、加诊断或完善工具；具体新反例、真实失败或受影响不变量才是继续调查的依据。

## 架构边界

- 按业务能力组织，再分领域、应用、基础设施和展示层；展示层 → 应用层 → 领域层，基础设施 → 应用端口与领域类型，装配根只负责依赖接线。
- 领域层不依赖框架、ORM、网络、文件系统或 SDK；应用层不依赖具体 ORM、HTTP/React 或浏览器对象；基础设施不导入展示代码。
- 跨能力只走命名公开 API、应用端口或稳定契约；禁止私有深层导入、循环依赖、运行时导入绕行和迁移调用运行期服务。
- 不建空架构目录或万能 utils/helpers/managers/services；共享代码须业务中立、语义稳定且已有至少两个真实消费者。
- 新增数据库能力使用 SQLAlchemy ORM 或类型化表达式；已定位的遗留 SQL 缺陷允许原位必要局部修正，边界见工程规范，不复制管线、不绕过参数绑定或授权，已发布迁移仍不可改写；稳定边界必须有显式类型与输入校验，生成文件不可手改。

## 强制复用与任务范围

- 写代码前按用户意图和行为检索既有入口、公开 API、状态所有者、适配器与测试。沿已知失败链定位唯一实现归属，信息明确时直接修正；仅在确有需要时扩展契约或提取共同逻辑，不先复制再合并，不重复无关盘点。
- 同一规则、流程、下载、认证、传输、校验、取消、持久化和重试机制只能有一个权威实现。平台适配器仅保留真实平台差异；可共享的移动业务归属 KMP，跨语言语义使用机器契约及一致性样例。
- 替换时迁移全部范围内调用方，删除旧实现、重复状态、废弃接线和隐藏回退；兼容层只能委托唯一实现，并有归属和明确移除条件。
- 一次围绕一个明确能力，不混入无关改版、升级、格式化或全仓迁移。范围外重复记录为发现；只有影响本次正确性、安全复用或必须突破边界时才请求范围决定，禁止自行增加副本。
- 不为局部缺陷新增通用框架、调度器、状态机、配置系统、数据模型或报告体系，不为“更优雅”“以后扩展”重写可用实现，也不为节省测试时间另建复杂工具。
- 不默认每个补丁创建独立工作树、委派审查代理或进行多轮相同复核；仅实际隔离需求、明确高风险或用户要求时增加步骤。
- 仅审计请求不修改应用代码。不得覆盖用户已有改动。

## 需求验证与缺陷排查

- 将显式行为变化贯穿入口、用例、适配器、网络/存储、回退和恢复；删按钮、改名称或转后台不能证明底层行为已移除。
- 移除行为时列明期望结果与禁止操作，按实际受影响路径检查首次空缓存、重试、取消、重新打开和恢复，验证禁止副作用不再发生。
- 相邻检查限于同一根因的直接消费者和实际受影响行为，避免逐页重复修复同一原因；不自动补齐整个功能面或扩大为全仓清理。
- 在固定 SDK 的真实调用链验证语义。流式声明须用尚未完成的慢响应证明提前消费及有界缓冲；不能以命名、加载提示、缓存命中或编译成功充当运行证据。具体 Reader 交付行为读取当前 ADR，不在此复制格式策略。

## 安全、状态与国际化底线

- 应用用例拥有授权、事务与副作用顺序，显式处理失败恢复；禁止隐藏提交、吞异常当成功、fire-and-forget 和无主后台任务。
- 外部输入先校验；按 actor/资源范围授权，保持既有反枚举契约。文件路径校验根目录与符号链接逃逸；日志与用户错误不得泄露秘密、正文、私有路径或堆栈。
- 状态只有一个所有者；异步操作有取消、过期结果拒绝和资源清理。保留键盘、触摸、焦点、无障碍与减少动态效果行为。
- 用户可见变化完成 zh-CN/en-US，包括非 DOM 输出；格式化使用当前 locale，用户内容不翻译，错误码与事件类型不依赖本地化文案。
- Reader 安全唯一来源为 [机器契约](packages/reader-contracts/reader-safety-policy.json)；禁止私有策略和手改生成绑定。变更流程与平台验证读取 ADR 0026。

## 测试、构建与完成要求

测试选择唯一遵循 [测试执行策略](docs/testing/test-execution-policy.md)：先划定受影响能力、调用方和风险，精确执行；不默认全量回归，不因检查通过后没有新疑点而重复扩大检查。静态检查工具缺少可靠局部模式时可运行所属模块/应用检查，不自动触发全仓测试。扩大范围须说明理由，不能削弱规则或跳过失败测试。

移动安装包按宿主默认分工：macOS → iOS，Windows → Android；明确要求可覆盖，受工具链与设备条件限制。Linux 按明确任务或既有 CI 目标。共享变更按影响检查共享能力，在必要验证阶段构建主机对应安装包，可合并同模块相关修复后统一验收，不因修改共享文件反复打包；记录另一平台实际验证缺口；不修改 CI 矩阵。iOS 仅真机，Android 默认真机，无设备明确记录运行验收待完成。操作步骤见移动端指南，不要求每次中间编译后安装。

完成按上述停止条件判断。简述实际修复、复用归属、有效验证与具体缺口；涉及迁移或替换时再说明调用方和删除项，不强制新建报告。范围内契约、授权、安全与双语要求保持；不能把编译或其他平台证据当作本平台运行验收。完成要求针对本次范围，不以无关遗留问题无限扩展任务。未运行全量回归不阻塞已充分验证的局部修复完成，但局部完成不等于最终发布通过；发布需版本一致和有效发布说明，按发布指南验证。

## 按任务读取

| 任务 | 必读来源 |
| --- | --- |
| Python、Web、数据库、重构 | [工程实现规范](docs/engineering-standards.md) 对应章节；[当前能力入口](docs/business-code-layering-and-refactoring.md) |
| 测试选择、回归、验收报告 | [测试执行策略](docs/testing/test-execution-policy.md) |
| 移动端 UI、平台构建、真机验收 | [移动端开发指南](docs/mobile-app-development-global-guidelines.md)，视觉数值读取 [视觉契约](packages/design-contracts/visual-tokens.json) |
| Mobile 目录与资源导航 | [内容导航规范](docs/mobile-book-content-navigation.md) |
| Reader 引擎、SDK、进度与设置 | [当前 Reader 架构](docs/mobile-reader-architecture.md) 及其链接的已接受 ADR、机器契约 |
| 原文件下载与阅读、PDF 交付 | [ADR 0025](docs/adr/0025-reflowable-original-download-before-reading.md)、[ADR 0027](docs/adr/0027-pdfium-transparent-original-materialization.md) |
| Reader 安全规则与一致性验证 | [ADR 0026](docs/adr/0026-versioned-reader-safety-policy-contract.md) 和机器契约 |
| 版本发布 | [发布指南](docs/release-guide.md) 与本次发布门禁 |
