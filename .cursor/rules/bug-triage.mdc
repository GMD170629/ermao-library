---
description: 功能缺陷按能力面扩展审计，而非只修字面症状。
alwaysApply: true
---

# 功能缺陷分诊

用户报告“某操作不工作”时，把它当作更大能力面的一例，审计用户自然期望的相邻交互。

1. 识别用户意图与功能面
2. 列出该面的期望交互变体
3. 区分已实现 / 部分实现 / 缺失
4. 获准实现时修报告问题及紧密相关缺失（范围过大则说明并收窄）；仅审计请求不得改应用代码
5. 最终回复点名检查过的能力面，不只复述症状

模式：`报告症状 → 期望能力集 → 实现审计`。适用于阅读器导航、导入、搜索筛选、整理、设置、进度同步、离线/PWA、移动布局等。

## 行为变更必须追到底层

遵守 `AGENTS.md` 的 **Requirement Fidelity and End-to-End Verification**。沿入口、用例、adapter、网络／存储、fallback 和恢复追踪，不得只删按钮／换路由／移到后台就声称旧行为已删除。旧 ADR 或历史测试不能覆盖用户明确的新要求。

在线阅读按 `AGENTS.md` 的 **Online Reading and Offline Download Separation** 验收：不能等待整文件下载，不能隐式转离线下载；需验证空缓存、慢速未完成传输、首屏、取消、重开与回退。分别核实网络流式、增量解析和按需渲染，函数名与分块写盘不能证明网络流式。
