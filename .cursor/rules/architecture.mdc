---
description: 业务能力优先分层、依赖方向与权威文档指针。所有新代码与触及存量能力时必须遵守。
alwaysApply: true
---

# 架构与依赖方向

权威实现政策：`AGENTS.md`。详细理由与分期迁移：`docs/business-code-layering-and-refactoring.md`。

存量遗留不是新代码的先例。新代码必须符合目标架构；改遗留时，把触及的能力迁向目标，行为保持兼容、范围限于请求的能力。若合规会显著扩大风险范围，停下来说明冲突，不要继续堆积架构债。

## 依赖方向（唯一允许）

```text
delivery/presentation -> application -> domain
infrastructure -> application ports / domain types
composition root -> 各层（仅装配）
```

## 禁止

- domain → 框架 / ORM / HTTP / 文件系统 / 队列 / 环境变量 / 第三方 SDK
- application → FastAPI Request/Response、React、Next.js、具体 SQLAlchemy 实现、浏览器全局
- infrastructure → route / page / component
- module/feature → 另一 module/feature 的私有文件
- 数据库迁移 → 运行期 application/service
- 新模块 → `compat.py` 或兼容层私有 helper
- 顶层万能 `utils` / `helpers` / `managers` / 通用 `services`
- 空目录脚手架；仅在迁入真实代码时建目录

跨能力协作只能走对方 `public` 入口、应用端口或稳定契约。禁止深层导入、循环导入、运行时 import 技巧。

## 强制复用

遵守 `AGENTS.md` 的 **Mandatory Reuse and Single Implementation**，理由与流程见分层规范第 3.3 节。

- 任何情况下先按行为检索已有实现，再编码；相同规则／流程／机制只能有一个 owner，不得复制或另写等价逻辑。
- 现有能力不够时，在正确所属层扩展契约或提取共同实现，所有相关入口调用同一 public API／端口；禁止先复制、以后合并。
- adapter 只保留真实平台／协议／格式差异；共享规则不得随页面、格式或平台重写。不得为复用破坏原生 UI 所有权或分层边界。
- 不把业务规则塞进通用 shared；遇到安全复用与分层／语言／不可变迁移／范围冲突时先报告并请求用户决策，禁止自行豁免。
- Reader 与 Download Center 不得各自维护完整下载链路。共同底层机制复用，在线阅读与显式离线下载的用户意图分离。

## 代理默认行为

1. 先识别业务能力，再按能力内分层落代码。
2. 不把一次改动混入架构迁移、产品改版、依赖升级和无关格式化。
3. 临时兼容代码必须有 owner 和明确退出条件。

- 普通在线阅读只按需获取原章节/页/PDF Range；应用缓存总量 ≤64 MiB、文本章节 ≤8 MiB、PDF 请求 ≤1 MiB。原生 App 准入为 2 GiB（含边界），明确在线限制可经独立过渡页复用 Downloads 后本地打开；普通网络、鉴权、损坏、版本变化不分流。允许原生 SDK 常规缓存，不深改 SDK，不拆章、不转换、不新增索引。Web 保持原严格策略和依赖补丁；所有平台禁止在线后台补齐原文件。详见 AGENTS.md 和 ADR 0024。
- Downloads 的共享应用用例是唯一显式下载入口。平台只适配存储、通知与生命周期；文件与多页下载复用传输机制。迁移全部调用后删除旧实现、别名、提示、测试预期及冲突规范。

- iOS Reader 当前批准基线为官方 Readium Swift Toolkit **3.9.0**，覆盖旧 3.8.0 冻结说明；不得为消除编译或迁移错误回退 SDK。精确 revision、SwiftPM 锁和运行时版本须通过 `python3 apps/mobile/iosApp/verify_readium.py`。遵守 `docs/mobile-reader-architecture.md` 第 10 节，不改 SDK 源码、不用私有 API、不恢复排版校验或加载 UI；Android/Web 版本不变。
