# v2 问题关闭与收敛记录

状态：PASS（视觉约定范围）。v1 APK 和原始基线完整保留；本轮最终 candidate-1 APK 为 `d905267a8db41ba174f6bde5874b668204bb671cdf684e5043b625982ca339bd`。

## 问题账本

| 编号 | 级别 | 症状 / 根因 | 修复与验收证据 | 状态 |
| --- | --- | --- | --- | --- |
| VIS2-001 | minor | 导航与底边有白色断层；平台自动 scrim 覆盖 App 导航表面 | 主 Activity 透明导航样式，由 Shell 填充手势 inset；01 / 14 / 20 / 21 四处底边和导航 RGB 均为 243,241,238 | closed |
| VIS2-002 | minor | 选中胶囊无上下内部留白；背景绘制在内容外 padding 之后但没有内 padding | 共享底部导航增加上下 6dp 内 padding，左右 4dp 外距；四个胶囊同为 332×197px，字形至背景上下至少 8dp | closed |
| VIS2-003 | minor | 设置列表过滤 Chip 与表单顶部 Tab 风格漂移；相同语义多实现 | 统一 SettingsTabRow owner 与固定 tabs 插槽；15–19、28–29 产品图，队列和导入任务的状态分组及禁用回调测试 | closed |
| VIS2-004 | minor | 长路径无法完整查看，视图/排序文字占宽且分行 | 路径展开 Sheet + 两枚同排图标按钮；02–09，根目录与祖先回调、长标题真实文字换行无裁切测试 | closed |
| VIS2-005 | minor | 图书/目录/卷册菜单过宽且缺少共享圆角 | 统一 WarmPagePopup 最大 224dp / task 16dp；ManagementAnchor 消除 280dp；08 / 10 / 11 / 13 / 22 / 23 真机复验 | closed |
| VIS2-006 | polish | Kindle All 空态标题和说明重复 | 仅移除重复说明，保留其他筛选上下文；15–17 | closed |

## 逐轴收敛

| must-match 轴 | v1 → candidate-1 | 依据 |
| --- | --- | --- |
| 页面区块与顺序 | same | 首页三分区、书库三列、书架/我的行式分组、详情阅读区与目录均保留 |
| 首屏折叠与信息密度 | better | 目录工具栏减少一行；首项标题从 y=2607–2856 提前至 y=2397–2646，完整内容仍可见 |
| 主封面 / 阅读区比例 | same | 根目录封面前后均 [552,417]–[888,921]px；单资源仍 120dp |
| 排版与字段内容 | same | 标题、作者、资源格式与排序数据保持；长路径改为展开后完整显示 |
| 表面与控件语言 | better | 菜单尺寸、圆角、设置过滤、图标工具栏统一到共享实现 |
| 颜色与 App Shell | better | 底边接缝消失，导航选中胶囊增加内部留白；正文起止位置不变 |
| 直接相邻消费者 | same | 四 Tab、账户、日志、用户、下载中心与菜单消费者未发现问题转移 |

没有 worse 轴。当前最高优先级的导航接缝和内部留白同时缩小；candidate-1 晋升。未覆盖旧基线，未调整权威目标。

## 复现与边界

原图和 APK 存放在本地私有 `core-tabs-polish-v2` 产物目录，run-manifest.json 记录每张原图哈希。报告入口 `http://127.0.0.1:8765/core-tabs-polish-v2/report.html`。

从冷启动首页依次进入最近入库第三本“鸡皮疙瘩系列”、第一 EPUB 子目录，点击目录名展开路径；右侧按钮切换列表/网格和排序；长按封面或点击更多展开菜单。我的 → Kindle 发送队列切换三种筛选；账户、日志和用户页作为相邻消费者。四 Tab 首/中/末选中均复拍。

所有闭环证据来自最终 APK 在 `9e896bbc` 上的中文、默认字号、Light、竖屏。没有发送邮件、保存账户或配置、下载或删除文件。工程检查与明确未验证路径详见 checks/verification.md；独立两阶段审查详见 independent-review.md。
