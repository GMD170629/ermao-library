# Android physical-device visual-polish ledger

Run scope: `Home -> Library -> Work Detail` on physical Android device `9e896bbc`.

Authority order: Mobile Phase 1-4 specifications, Phase 5 page anchors, Phase 7 acceptance contract, then platform-native ownership guidance. The untracked Work Detail v4 image and prior emulator report are not acceptance authority for this run.

| ID | Evidence | Symptom and consequence | Root cause / authority | Severity | Planned pass criterion | State |
| --- | --- | --- | --- | --- | --- | --- |
| VIS-001 | `baseline/android/home-library-work-detail/zh-CN-light-default/00-current-entry.png` | The Continue task is taller and noisier than the rest of Home. Long work/volume labels plus a raw ISO timestamp compete with the primary task, leaving little recent content visible. | Home presentation does not normalize the timestamp or suppress duplicate context. Phase 3/4 require one dominant continuation task with compact secondary metadata. | Major | Locale-aware human time, no raw transport timestamp, no duplicate title context, and a clear primary hierarchy on real data and deterministic fixtures. | Open |
| VIS-002 | `baseline/android/home-library-work-detail/zh-CN-light-default/02-work-detail-top.png`; `baseline/android/fixture-matrix/visual-fixtures/work-volumes-zh-CN-light.png` | Work Detail uses a non-2:3 hero and raised nested cards for About/Volumes/Chapters. A single volume becomes a large empty panel, weakening hierarchy and density. | Work-detail-only cover aspect and `WorkDetailCard` elevation/border diverge from Phase 4/5: all covers are 2:3, only covers may cast elevation, ordinary content is flat with whitespace/dividers. | Major | 2:3 hero; zero nested raised content cards; single and multi-volume layouts remain dense, readable, and consistent at default and 200% font scale. | Open |
| VIS-003 | `baseline/android/fixture-matrix/visual-fixtures/work-volumes-en-US-fontScale-2.png` | At 200% font scale the media selector and trailing metadata wrap/truncate awkwardly, making the screen look broken even though it remains technically reachable. | Fixed horizontal distribution is not large-text adaptive. Phase 7 requires default and maximum text without clipping or ambiguous controls. | Major | No clipped label, no ambiguous truncated action, and every target remains reachable at 200% in zh-CN and en-US. | Open |
| QA-001 | Physical instrumentation failure before baseline matrix | `VisualFixtureActivity` crashed on API 31 because `Activity.intent` is unavailable during `attachBaseContext`, then resource override was attempted after resources had already been resolved. | The debug-only host coupled an intent contract to an incompatible Activity lifecycle point. | Blocker | Fixture launch succeeds on the physical API 31 device for all variants without mutating app/session data. | Fixed; final full-matrix verification pending |
| QA-002 | Full fixture run after QA-001 fix | English Work Actions rendered correctly, but the test searched for the Chinese system-locale content description and failed before capture. | Overlay locator resolved strings from the Activity/system context rather than the explicit fixture locale. | Major | Overlay locators and rendered resources share the same explicit variant locale; all 28 default captures complete. | Fixed; final full-matrix verification pending |
| OBS-001 | `baseline/android/home-library-work-detail/zh-CN-light-default/07-library-bottom.png`; `08-library-end.png` | One end-of-list image lacks both app top and bottom chrome; the preceding ordinary viewport contains both. | Source has no navigation-hide state. The image may be a MIUI scroll-capture/content-layer artifact; do not change product without a reproducible normal-viewport defect. | Observation | Repeat ordinary `adb screencap` at list end and isolate with a Compose scroll test. Escalate only if app chrome disappears in the real viewport. | Diagnosing |
| FUNC-001 | `iteration-01/android/home-library-work-detail/zh-CN-light-default/02-work-detail.png` | An audiobook shows an enabled “Continue Listening” CTA while the build has no audio-player destination, and the supporting caption incorrectly discusses PDF/comic renderers. | The generic reader-entry policy and one shared caption are being reused for the distinct audiobook capability. Phase 3 assigns audiobooks to A07 Now Playing. | Major | Product choice is explicit: either deliver A07, or disable the unavailable CTA with accurate zh-CN/en-US copy. Never leave an enabled dead-end with unrelated copy. | Awaiting user choice; default proposal is truthful disabled state |

An issue closes only after a fresh build is replace-installed without clearing data, the physical device is cold-launched, the exact state is recaptured, adjacent variants pass, and the screenshot is reviewed against the authority above.
