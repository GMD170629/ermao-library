import type { ReactNode } from 'react';

import { ServerAddressFlowScreen } from '../../features/app-flow/public';

export default function ServerAddressRoute(): ReactNode {
  return <ServerAddressFlowScreen />;
}
